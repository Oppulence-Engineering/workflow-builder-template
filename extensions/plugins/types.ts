/**
 * Extension Plugin Types
 *
 * Additional types for fork-specific plugins
 */

export type ExtensionPluginConfig = {
  // Add fork-specific plugin configuration types here
  customField?: string;
};
